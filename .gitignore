data/
.env
__pycache__/
*.pyc
node_modules/
notebooks/
claude.md
.venv/
# Removed code kept for local reference only — never pushed
_archive/

# Claude Code tooling (third-party skills, local config) — not part of the app
.claude/
